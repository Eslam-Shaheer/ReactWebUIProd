/**
 * this function takes 2 args of type number and returns sum of both arguments
 * @param {number} num1
 * @param {number} num2
 * @returns number
 */
const sum = (num1, num2) => {
  return num1 + num2;
};

export default sum;
